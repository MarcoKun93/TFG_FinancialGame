<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dam.financialgame">

    <!-- Internet permission needed to call the API -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_CALENDAR" />

    <application
        android:name=".threads.VolleyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".controllers.InicializarPartida" />
        <activity android:name=".controllers.MenuDeInicio" />
        <activity
            android:name=".controllers.Comunidad"
            android:label="Comunidad" />
        <activity android:name=".controllers.Escenario" />
        <activity android:name=".controllers.IntroducirDatos" />
        <activity android:name=".controllers.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".controllers.GraficasJugadores" />
        <activity android:name=".controllers.LineGraph" />
        <activity android:name=".controllers.PieGraph" />
        <activity android:name=".controllers.ManualesDeUsuario" />
        <activity android:name=".controllers.ManualDeUsuario"></activity>
    </application>

</manifest>